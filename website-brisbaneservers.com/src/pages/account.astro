---
export const prerender = true;

import AccountWorkspacePage from '../components/account/AccountWorkspacePage.astro';

const initialVerifyToken = Astro.url.searchParams.get('verify') ?? '';
const initialResetToken = Astro.url.searchParams.get('reset') ?? '';
---

<AccountWorkspacePage initialVerifyToken={initialVerifyToken} initialResetToken={initialResetToken} />
