/**
 * Single source of truth for production URLs (Pages, Worker API, site).
 * Docs and scripts should reference these constants or the paths below.
 */

export const CANONICAL_SITE_URL = 'https://brisbaneservers.com';
export const CANONICAL_API_BASE_URL = 'https://api.brisbaneservers.com/api';
export const CANONICAL_API_ORIGIN = 'https://api.brisbaneservers.com';

/** @deprecated Render API retired — kept for legacy test fixtures only */
export const LEGACY_RENDER_API_BASE_URL = 'https://brisbane-servers-api.onrender.com/api';
